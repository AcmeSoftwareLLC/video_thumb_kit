import 'dart:typed_data';

class VideoThumbKitWeb {
  Future<Uint8List?> thumbnailDataWeb({
    required Uint8List videoBytes,
    num quality = 100,
  }) {
    throw UnimplementedError('thumbnailDataWeb() has not been implemented.');
  }
}
